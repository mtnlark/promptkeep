{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"PromptKeep","text":"<p>A command-line tool for managing reusable AI prompts.</p> <p>PromptKeep stores prompts as Markdown files with YAML metadata, letting you organize, search, and copy them to your clipboard with a single command.</p>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code># Install\ngit clone https://github.com/mtnlark/promptkeep.git\ncd promptkeep\npip install .\n\n# Create a vault\npromptkeep init\n\n# Add a prompt\npromptkeep add --title \"Code Review\"\n\n# Find and copy a prompt\npromptkeep pick\n</code></pre>"},{"location":"#why-promptkeep","title":"Why PromptKeep?","text":"<ul> <li>Fast access: Fuzzy search finds prompts instantly</li> <li>Organized: Tag-based filtering keeps prompts manageable</li> <li>Portable: Plain Markdown files work anywhere</li> <li>Private: Local storage, no cloud dependencies</li> <li>Compatible: Works with Obsidian and other Markdown tools</li> </ul>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Installation \u2014 Prerequisites and setup options</li> <li>Usage Guide \u2014 Commands and workflows</li> <li>Reference \u2014 Technical details and configuration</li> </ul>"},{"location":"installation/","title":"Installation","text":""},{"location":"installation/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.8+</li> <li>fzf \u2014 fuzzy finder for prompt selection</li> </ul>"},{"location":"installation/#installing-fzf","title":"Installing fzf","text":"macOSUbuntu/DebianFedoraWindows <pre><code>brew install fzf\n</code></pre> <pre><code>sudo apt install fzf\n</code></pre> <pre><code>sudo dnf install fzf\n</code></pre> <pre><code># With Chocolatey\nchoco install fzf\n\n# With Scoop\nscoop install fzf\n</code></pre>"},{"location":"installation/#install-promptkeep","title":"Install PromptKeep","text":"<pre><code>git clone https://github.com/mtnlark/promptkeep.git\ncd promptkeep\npip install .\n</code></pre>"},{"location":"installation/#using-a-virtual-environment-recommended","title":"Using a Virtual Environment (Recommended)","text":"<pre><code>git clone https://github.com/mtnlark/promptkeep.git\ncd promptkeep\npython -m venv .venv\nsource .venv/bin/activate  # Windows: .venv\\Scripts\\activate\npip install .\n</code></pre>"},{"location":"installation/#verify-installation","title":"Verify Installation","text":"<pre><code>promptkeep --help\n</code></pre> <p>You should see the available commands listed.</p>"},{"location":"installation/#configuration","title":"Configuration","text":"<p>PromptKeep uses two environment variables:</p> Variable Purpose Default <code>PROMPTKEEP_VAULT</code> Vault location <code>~/PromptVault</code> <code>EDITOR</code> Text editor for editing prompts <code>vim</code> <p>Set these in your shell profile (<code>.bashrc</code>, <code>.zshrc</code>, etc.):</p> <pre><code>export PROMPTKEEP_VAULT=~/Documents/Prompts\nexport EDITOR=code  # or vim, nano, etc.\n</code></pre>"},{"location":"installation/#troubleshooting","title":"Troubleshooting","text":"\"fzf not found\" Install fzf using the instructions above, then ensure it's in your PATH. \"command not found: promptkeep\" If using a virtual environment, make sure it's activated. Otherwise, check that your Python scripts directory is in your PATH. Editor doesn't open Set the <code>EDITOR</code> environment variable to your preferred editor."},{"location":"reference/","title":"Reference","text":"<p>Technical details for PromptKeep commands, configuration, and architecture.</p>"},{"location":"reference/#commands","title":"Commands","text":""},{"location":"reference/#init","title":"init","text":"<p>Create a new prompt vault.</p> <pre><code>promptkeep init [PATH]\n</code></pre> Argument Default Description <code>PATH</code> <code>~/PromptVault</code> Vault location <p>Warning</p> <p>Overwrites existing vault at the specified path.</p>"},{"location":"reference/#add","title":"add","text":"<p>Add a new prompt.</p> <pre><code>promptkeep add [OPTIONS]\n</code></pre> Option Required Description <code>--title</code>, <code>-t</code> Yes Prompt title <code>--description</code>, <code>-d</code> No Prompt description <code>--tag</code> No Tag (repeatable) <code>--vault</code>, <code>-v</code> No Vault path"},{"location":"reference/#pick","title":"pick","text":"<p>Select a prompt and copy to clipboard.</p> <pre><code>promptkeep pick [OPTIONS]\n</code></pre> Option Description <code>--tag</code>, <code>-t</code> Filter by tag (repeatable, AND logic) <code>--vault</code>, <code>-v</code> Vault path"},{"location":"reference/#edit","title":"edit","text":"<p>Edit an existing prompt.</p> <pre><code>promptkeep edit [OPTIONS]\n</code></pre> Option Description <code>--tag</code>, <code>-t</code> Filter by tag (repeatable, AND logic) <code>--vault</code>, <code>-v</code> Vault path"},{"location":"reference/#configuration","title":"Configuration","text":""},{"location":"reference/#environment-variables","title":"Environment Variables","text":"Variable Default Description <code>PROMPTKEEP_VAULT</code> <code>~/PromptVault</code> Default vault location <code>EDITOR</code> <code>vim</code> Editor for prompt editing"},{"location":"reference/#precedence","title":"Precedence","text":"<p>Configuration resolves in this order:</p> <ol> <li>Command-line arguments (<code>--vault</code>)</li> <li>Environment variables (<code>PROMPTKEEP_VAULT</code>)</li> <li>Default values</li> </ol>"},{"location":"reference/#file-format","title":"File Format","text":""},{"location":"reference/#prompt-structure","title":"Prompt Structure","text":"<pre><code>---\ntitle: \"Prompt Title\"\ndescription: \"Optional description\"\ntags: [\"tag1\", \"tag2\"]\n---\n\nPrompt content goes here.\n</code></pre>"},{"location":"reference/#filename-format","title":"Filename Format","text":"<pre><code>{sanitized-title}-{YYYYMMDD-HHMMSS}.md\n</code></pre> <p>Example: <code>code-review-20240315-143022.md</code></p>"},{"location":"reference/#vault-structure","title":"Vault Structure","text":"<pre><code>VaultPath/\n\u2514\u2500\u2500 Prompts/\n    \u251c\u2500\u2500 example-prompt.md\n    \u2514\u2500\u2500 code-review-20240315-143022.md\n</code></pre>"},{"location":"reference/#exit-codes","title":"Exit Codes","text":"Code Meaning 0 Success 1 Error (vault not found, fzf not installed, etc.)"},{"location":"reference/#architecture","title":"Architecture","text":"<p>PromptKeep uses dependency injection and protocol-based interfaces for testability.</p>"},{"location":"reference/#modules","title":"Modules","text":"Module Purpose <code>cli</code> Command-line interface <code>config</code> Configuration management <code>context</code> Dependency injection container <code>exceptions</code> Custom exception hierarchy <code>models</code> Data models <code>protocols</code> Service interfaces <code>repository</code> Data access layer <code>services</code> External integrations (clipboard, editor, fzf) <code>utils</code> Utility functions"},{"location":"reference/#exception-hierarchy","title":"Exception Hierarchy","text":"<pre><code>PromptKeepError\n\u251c\u2500\u2500 VaultNotFoundError\n\u251c\u2500\u2500 VaultInvalidError\n\u251c\u2500\u2500 EditorError\n\u2502   \u2514\u2500\u2500 EditorNotFoundError\n\u2514\u2500\u2500 SelectorError\n    \u2514\u2500\u2500 SelectorNotFoundError\n</code></pre>"},{"location":"reference/#dependencies","title":"Dependencies","text":"Package Purpose typer CLI framework rich Terminal formatting pyperclip Clipboard access pyyaml YAML parsing <p>External: fzf (fuzzy finder)</p>"},{"location":"usage/","title":"Usage Guide","text":""},{"location":"usage/#initialize-a-vault","title":"Initialize a Vault","text":"<p>Create a vault to store your prompts:</p> <pre><code>promptkeep init\n</code></pre> <p>This creates <code>~/PromptVault</code> with a <code>Prompts</code> directory and an example file.</p> <p>To use a different location:</p> <pre><code>promptkeep init ~/Documents/MyPrompts\n</code></pre>"},{"location":"usage/#add-prompts","title":"Add Prompts","text":""},{"location":"usage/#interactive-mode","title":"Interactive Mode","text":"<pre><code>promptkeep add\n</code></pre> <p>You'll be prompted for a title, description, and tags. Your editor opens to write the prompt content.</p>"},{"location":"usage/#command-line","title":"Command Line","text":"<pre><code>promptkeep add --title \"Code Review\" --tag coding --tag review\n</code></pre>"},{"location":"usage/#prompt-format","title":"Prompt Format","text":"<p>Prompts are Markdown files with YAML front matter:</p> <pre><code>---\ntitle: \"Code Review\"\ndescription: \"Analyze code for issues and improvements\"\ntags: [\"coding\", \"review\"]\n---\n\nReview this code for:\n1. Bugs and edge cases\n2. Performance issues\n3. Security vulnerabilities\n4. Readability improvements\n\n[PASTE CODE HERE]\n</code></pre>"},{"location":"usage/#find-and-copy-prompts","title":"Find and Copy Prompts","text":"<pre><code>promptkeep pick\n</code></pre> <p>This opens a fuzzy finder. Type to search, use arrow keys to navigate, press Enter to copy the selected prompt to your clipboard.</p>"},{"location":"usage/#filter-by-tag","title":"Filter by Tag","text":"<pre><code>promptkeep pick --tag coding\npromptkeep pick --tag coding --tag python  # Must have both tags\n</code></pre>"},{"location":"usage/#edit-prompts","title":"Edit Prompts","text":"<pre><code>promptkeep edit\n</code></pre> <p>Select a prompt from the fuzzy finder to open it in your editor.</p> <p>Filter by tag:</p> <pre><code>promptkeep edit --tag coding\n</code></pre>"},{"location":"usage/#use-a-different-vault","title":"Use a Different Vault","text":"<p>Override the default vault for any command:</p> <pre><code>promptkeep pick --vault ~/Work/Prompts\n</code></pre> <p>Or set the environment variable:</p> <pre><code>export PROMPTKEEP_VAULT=~/Work/Prompts\n</code></pre>"},{"location":"usage/#tagging-strategies","title":"Tagging Strategies","text":"<p>Consistent tags make prompts easier to find. Consider organizing by:</p> Category Example Tags Task type <code>coding</code>, <code>writing</code>, <code>research</code> Domain <code>python</code>, <code>sql</code>, <code>marketing</code> Frequency <code>daily</code>, <code>reference</code> <p>Combine tags to narrow searches:</p> <pre><code>promptkeep pick --tag coding --tag python\n</code></pre>"},{"location":"usage/#tips","title":"Tips","text":"<p>Use placeholders \u2014 Mark areas to fill in: <code>[CLIENT NAME]</code>, <code>[PASTE CODE]</code></p> <p>Favorite prompts \u2014 Tag frequently used prompts with <code>fav</code> for quick access: <pre><code>promptkeep pick --tag fav\n</code></pre></p> <p>Keyboard shortcuts in fzf:</p> <ul> <li><code>Ctrl+N</code> / <code>Ctrl+P</code> \u2014 Navigate up/down</li> <li><code>Ctrl+C</code> / <code>Esc</code> \u2014 Cancel</li> <li><code>Enter</code> \u2014 Select</li> </ul>"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/","title":"Architecture Improvements Implementation Plan","text":"<p>For agentic workers: REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (<code>- [ ]</code>) syntax for tracking.</p> <p>Goal: Refactor PromptKeep to showcase dependency injection, configuration management, and protocol-based interfaces.</p> <p>Architecture: Introduce AppContext as a composition root that wires together services (clipboard, editor, selector) defined by Protocol interfaces. Configuration is centralized in an immutable Config dataclass. Custom exceptions replace scattered error handling.</p> <p>Tech Stack: Python 3.8+, Typer, Rich, dataclasses, typing.Protocol</p> <p>Spec: <code>docs/superpowers/specs/2026-03-23-architecture-improvements-design.md</code></p>"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#file-structure","title":"File Structure","text":""},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#new-files","title":"New Files","text":"File Responsibility <code>src/promptkeep/exceptions.py</code> Custom exception hierarchy <code>src/promptkeep/protocols.py</code> Service interface definitions <code>src/promptkeep/config.py</code> Configuration management <code>src/promptkeep/services.py</code> Concrete service implementations <code>src/promptkeep/context.py</code> AppContext composition root <code>tests/test_exceptions.py</code> Exception tests <code>tests/test_config.py</code> Configuration tests <code>tests/test_services.py</code> Service implementation tests <code>tests/test_context.py</code> Context creation tests"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#modified-files","title":"Modified Files","text":"File Changes <code>src/promptkeep/constants.py</code> Add FZF_PREVIEW_SCRIPT <code>src/promptkeep/cli.py</code> Use AppContext, add error decorator <code>src/promptkeep/utils.py</code> Remove functions moved to services <code>tests/test_cli.py</code> Fix linting, update for new patterns"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#task-1-create-exception-hierarchy","title":"Task 1: Create Exception Hierarchy","text":"<p>Files: - Create: <code>src/promptkeep/exceptions.py</code> - Create: <code>tests/test_exceptions.py</code></p> <ul> <li> Step 1.1: Write exception tests</li> </ul> <pre><code># tests/test_exceptions.py\n\"\"\"Tests for custom exception hierarchy.\"\"\"\nfrom pathlib import Path\n\nimport pytest\n\nfrom promptkeep.exceptions import (\n    PromptKeepError,\n    VaultNotFoundError,\n    VaultInvalidError,\n    EditorError,\n    EditorNotFoundError,\n    SelectorError,\n    SelectorNotFoundError,\n)\n\n\nclass TestExceptionHierarchy:\n    \"\"\"Test that exceptions inherit correctly.\"\"\"\n\n    def test_vault_not_found_is_promptkeep_error(self):\n        \"\"\"VaultNotFoundError should inherit from PromptKeepError.\"\"\"\n        assert issubclass(VaultNotFoundError, PromptKeepError)\n\n    def test_vault_invalid_is_promptkeep_error(self):\n        \"\"\"VaultInvalidError should inherit from PromptKeepError.\"\"\"\n        assert issubclass(VaultInvalidError, PromptKeepError)\n\n    def test_editor_error_is_promptkeep_error(self):\n        \"\"\"EditorError should inherit from PromptKeepError.\"\"\"\n        assert issubclass(EditorError, PromptKeepError)\n\n    def test_editor_not_found_is_editor_error(self):\n        \"\"\"EditorNotFoundError should inherit from EditorError.\"\"\"\n        assert issubclass(EditorNotFoundError, EditorError)\n\n    def test_selector_error_is_promptkeep_error(self):\n        \"\"\"SelectorError should inherit from PromptKeepError.\"\"\"\n        assert issubclass(SelectorError, PromptKeepError)\n\n    def test_selector_not_found_is_selector_error(self):\n        \"\"\"SelectorNotFoundError should inherit from SelectorError.\"\"\"\n        assert issubclass(SelectorNotFoundError, SelectorError)\n\n\nclass TestVaultNotFoundError:\n    \"\"\"Tests for VaultNotFoundError.\"\"\"\n\n    def test_stores_searched_paths(self):\n        \"\"\"Should store the paths that were searched.\"\"\"\n        paths = [Path(\"/path/one\"), Path(\"/path/two\")]\n        err = VaultNotFoundError(paths)\n        assert err.searched_paths == paths\n\n    def test_default_empty_paths(self):\n        \"\"\"Should default to empty list if no paths provided.\"\"\"\n        err = VaultNotFoundError()\n        assert err.searched_paths == []\n\n    def test_message(self):\n        \"\"\"Should have descriptive message.\"\"\"\n        err = VaultNotFoundError()\n        assert \"vault\" in str(err).lower()\n\n\nclass TestVaultInvalidError:\n    \"\"\"Tests for VaultInvalidError.\"\"\"\n\n    def test_stores_vault_path(self):\n        \"\"\"Should store the invalid vault path.\"\"\"\n        path = Path(\"/invalid/vault\")\n        err = VaultInvalidError(path)\n        assert err.vault_path == path\n\n    def test_message_includes_path(self):\n        \"\"\"Message should include the vault path.\"\"\"\n        path = Path(\"/invalid/vault\")\n        err = VaultInvalidError(path)\n        assert str(path) in str(err)\n\n\nclass TestEditorNotFoundError:\n    \"\"\"Tests for EditorNotFoundError.\"\"\"\n\n    def test_stores_editor_name(self):\n        \"\"\"Should store the editor name.\"\"\"\n        err = EditorNotFoundError(\"nvim\")\n        assert err.editor == \"nvim\"\n\n    def test_message_includes_editor(self):\n        \"\"\"Message should include editor name.\"\"\"\n        err = EditorNotFoundError(\"nvim\")\n        assert \"nvim\" in str(err)\n\n\nclass TestSelectorNotFoundError:\n    \"\"\"Tests for SelectorNotFoundError.\"\"\"\n\n    def test_message_mentions_fzf(self):\n        \"\"\"Message should mention fzf.\"\"\"\n        err = SelectorNotFoundError()\n        assert \"fzf\" in str(err).lower()\n</code></pre> <ul> <li> Step 1.2: Run tests to verify they fail</li> </ul> <p>Run: <code>pytest tests/test_exceptions.py -v</code> Expected: FAIL with \"ModuleNotFoundError: No module named 'promptkeep.exceptions'\"</p> <ul> <li> Step 1.3: Implement exceptions module</li> </ul> <pre><code># src/promptkeep/exceptions.py\n\"\"\"Custom exception hierarchy for PromptKeep.\n\nThis module defines a hierarchy of exceptions that provide clear,\nactionable error messages. All exceptions inherit from PromptKeepError,\nmaking it easy to catch all application errors.\n\"\"\"\nfrom pathlib import Path\nfrom typing import List, Optional\n\n\nclass PromptKeepError(Exception):\n    \"\"\"Base exception for all PromptKeep errors.\"\"\"\n\n    pass\n\n\nclass VaultNotFoundError(PromptKeepError):\n    \"\"\"Raised when no vault directory can be found.\"\"\"\n\n    def __init__(self, searched_paths: Optional[List[Path]] = None):\n        self.searched_paths = searched_paths or []\n        super().__init__(\"No vault found\")\n\n\nclass VaultInvalidError(PromptKeepError):\n    \"\"\"Raised when vault exists but is malformed (missing Prompts dir).\"\"\"\n\n    def __init__(self, vault_path: Path):\n        self.vault_path = vault_path\n        super().__init__(f\"Invalid vault at {vault_path}: missing Prompts directory\")\n\n\nclass EditorError(PromptKeepError):\n    \"\"\"Raised when the editor fails to open or exits with error.\"\"\"\n\n    pass\n\n\nclass EditorNotFoundError(EditorError):\n    \"\"\"Raised when the configured editor is not found.\"\"\"\n\n    def __init__(self, editor: str):\n        self.editor = editor\n        super().__init__(f\"Editor not found: {editor}\")\n\n\nclass SelectorError(PromptKeepError):\n    \"\"\"Raised when the prompt selector fails.\"\"\"\n\n    pass\n\n\nclass SelectorNotFoundError(SelectorError):\n    \"\"\"Raised when fzf is not installed.\"\"\"\n\n    def __init__(self):\n        super().__init__(\"fzf not found. Please install fzf to use this feature.\")\n</code></pre> <ul> <li> Step 1.4: Run tests to verify they pass</li> </ul> <p>Run: <code>pytest tests/test_exceptions.py -v</code> Expected: All tests PASS</p> <ul> <li> Step 1.5: Run linter</li> </ul> <p>Run: <code>ruff check src/promptkeep/exceptions.py tests/test_exceptions.py</code> Expected: No errors</p> <ul> <li> Step 1.6: Commit</li> </ul> <pre><code>git add src/promptkeep/exceptions.py tests/test_exceptions.py\ngit commit -m \"feat: add custom exception hierarchy\n\nAdd PromptKeepError base class with specialized exceptions:\n- VaultNotFoundError, VaultInvalidError for vault issues\n- EditorError, EditorNotFoundError for editor issues\n- SelectorError, SelectorNotFoundError for fzf issues\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#task-2-create-protocol-interfaces","title":"Task 2: Create Protocol Interfaces","text":"<p>Note on testing: Protocol classes define interfaces via structural typing - they have no runtime behavior to test. Verification that the module loads and type-checks is sufficient. The services that implement these protocols are tested in Task 5.</p> <p>Files: - Create: <code>src/promptkeep/protocols.py</code></p> <ul> <li> Step 2.1: Create protocols module</li> </ul> <pre><code># src/promptkeep/protocols.py\n\"\"\"Protocol definitions for PromptKeep services.\n\nProtocols define the interfaces that services must implement. This enables\ndependency injection and makes the code easier to test by allowing mock\nimplementations.\n\nUses typing.Protocol for structural subtyping - classes don't need to\nexplicitly inherit from these protocols, they just need to implement\nthe required methods.\n\"\"\"\nfrom pathlib import Path\nfrom typing import List, Optional, Protocol\n\nfrom promptkeep.models import Prompt\n\n\nclass ClipboardService(Protocol):\n    \"\"\"Interface for clipboard operations.\"\"\"\n\n    def copy(self, text: str) -&gt; None:\n        \"\"\"Copy text to the system clipboard.\"\"\"\n        ...\n\n\nclass EditorService(Protocol):\n    \"\"\"Interface for opening files in an editor.\n\n    Implementations should raise EditorNotFoundError if the editor binary\n    is not found, or EditorError for other failures.\n    \"\"\"\n\n    def open(self, file_path: Path) -&gt; bool:\n        \"\"\"Open a file in the editor. Returns True on success.\"\"\"\n        ...\n\n\nclass PromptSelector(Protocol):\n    \"\"\"Interface for interactive prompt selection.\n\n    Implementations should raise SelectorNotFoundError if the selection\n    tool (e.g., fzf) is not installed.\n    \"\"\"\n\n    def select(self, items: List[Path], prompt: str) -&gt; Optional[Path]:\n        \"\"\"Show interactive selection UI. Returns None if user cancels.\"\"\"\n        ...\n\n\nclass PromptRepositoryProtocol(Protocol):\n    \"\"\"Interface for prompt storage operations.\n\n    This is a read-heavy interface reflecting the primary use case of\n    browsing and selecting prompts. Write operations (save) are secondary.\n    Delete is intentionally omitted - users manage files directly.\n    \"\"\"\n\n    def list_all(self) -&gt; List[Prompt]:\n        \"\"\"Get all prompts from the vault.\"\"\"\n        ...\n\n    def filter_by_tags(self, tags: List[str]) -&gt; List[Prompt]:\n        \"\"\"Get prompts that have all specified tags.\"\"\"\n        ...\n\n    def get_by_path(self, filepath: Path) -&gt; Prompt:\n        \"\"\"Load a prompt from a file path.\n\n        Raises:\n            FileNotFoundError: If the file doesn't exist\n            OSError: For permission or I/O issues\n        \"\"\"\n        ...\n\n    def get_file_paths(self, tags: Optional[List[str]] = None) -&gt; List[Path]:\n        \"\"\"Get file paths, optionally filtered by tags.\"\"\"\n        ...\n\n    def save(self, prompt: Prompt, filename: Optional[str] = None) -&gt; Path:\n        \"\"\"Save a prompt to a file. Returns the path.\"\"\"\n        ...\n\n    def exists_similar(self, title: str) -&gt; List[Path]:\n        \"\"\"Check if prompts with similar titles exist.\"\"\"\n        ...\n</code></pre> <ul> <li> Step 2.2: Verify protocols module loads</li> </ul> <p>Run: <code>python -c \"from promptkeep.protocols import ClipboardService, EditorService, PromptSelector, PromptRepositoryProtocol; print('OK')\"</code> Expected: \"OK\"</p> <ul> <li> Step 2.3: Run linter</li> </ul> <p>Run: <code>ruff check src/promptkeep/protocols.py</code> Expected: No errors</p> <ul> <li> Step 2.4: Commit</li> </ul> <pre><code>git add src/promptkeep/protocols.py\ngit commit -m \"feat: add Protocol interfaces for services\n\nDefine structural typing interfaces:\n- ClipboardService for clipboard operations\n- EditorService for file editing\n- PromptSelector for interactive selection\n- PromptRepositoryProtocol for storage operations\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#task-3-update-constants-with-fzf-preview-script","title":"Task 3: Update Constants with FZF Preview Script","text":"<p>Files: - Modify: <code>src/promptkeep/constants.py</code></p> <ul> <li> Step 3.1: Add FZF_PREVIEW_SCRIPT to constants</li> </ul> <p>Add to end of <code>src/promptkeep/constants.py</code>:</p> <pre><code># FZF preview script - extracts title, tags and content from prompt files\n# The {} placeholder is substituted by fzf with the currently highlighted file\nFZF_PREVIEW_SCRIPT = \"\"\"awk '\n    BEGIN { in_yaml=0; printed_header=0 }\n    /^---$/ { in_yaml = !in_yaml; next }\n    in_yaml {\n        if ($1 == \"title:\") title = substr($0, 8)\n        if ($1 == \"tags:\") { tags=1; next }\n        if (tags &amp;&amp; $1 == \"-\") tag_list = tag_list \", \" substr($0, 3)\n    }\n    !in_yaml &amp;&amp; !printed_header {\n        gsub(/\"/, \"\", title)\n        sub(/, /, \"\", tag_list)\n        print \"Title: \" title\n        if (tag_list) print \"Tags:  \" tag_list\n        print \"----------------------------------------\"\n        printed_header=1\n        next\n    }\n    !in_yaml { print }\n' {}'\"\"\"\n</code></pre> <ul> <li> Step 3.2: Verify constant loads</li> </ul> <p>Run: <code>python -c \"from promptkeep.constants import FZF_PREVIEW_SCRIPT; print('OK' if '{}' in FZF_PREVIEW_SCRIPT else 'FAIL')\"</code> Expected: \"OK\"</p> <ul> <li> Step 3.3: Commit</li> </ul> <pre><code>git add src/promptkeep/constants.py\ngit commit -m \"feat: move FZF preview script to constants\n\nCentralizes the awk script used for fzf preview, avoiding\ncircular imports when used by services module.\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#task-4-create-configuration-module","title":"Task 4: Create Configuration Module","text":"<p>Files: - Create: <code>src/promptkeep/config.py</code> - Create: <code>tests/test_config.py</code></p> <ul> <li> Step 4.1: Write config tests</li> </ul> <pre><code># tests/test_config.py\n\"\"\"Tests for configuration management.\"\"\"\nimport os\nfrom pathlib import Path\nfrom unittest.mock import patch\n\nimport pytest\n\nfrom promptkeep.config import Config\nfrom promptkeep.exceptions import VaultNotFoundError, VaultInvalidError\n\n\nclass TestConfigCreation:\n    \"\"\"Tests for Config dataclass.\"\"\"\n\n    def test_config_is_immutable(self):\n        \"\"\"Config should be frozen (immutable).\"\"\"\n        config = Config(vault_path=Path(\"/test\"), editor=\"vim\")\n        with pytest.raises(AttributeError):\n            config.vault_path = Path(\"/other\")\n\n    def test_prompts_dir_property(self):\n        \"\"\"prompts_dir should return vault_path / Prompts.\"\"\"\n        config = Config(vault_path=Path(\"/test/vault\"), editor=\"vim\")\n        assert config.prompts_dir == Path(\"/test/vault/Prompts\")\n\n    def test_custom_prompts_dir_name(self):\n        \"\"\"Should support custom prompts directory name.\"\"\"\n        config = Config(\n            vault_path=Path(\"/test\"),\n            editor=\"vim\",\n            prompts_dir_name=\"MyPrompts\",\n        )\n        assert config.prompts_dir == Path(\"/test/MyPrompts\")\n\n\nclass TestConfigFromEnvironment:\n    \"\"\"Tests for Config.from_environment().\"\"\"\n\n    def test_uses_default_vault_path(self):\n        \"\"\"Should use ~/PromptVault when no override or env var.\"\"\"\n        with patch.dict(os.environ, {}, clear=True):\n            config = Config.from_environment()\n            assert config.vault_path == Path.home() / \"PromptVault\"\n\n    def test_vault_override_takes_precedence(self):\n        \"\"\"Explicit vault_override should override env var.\"\"\"\n        with patch.dict(os.environ, {\"PROMPTKEEP_VAULT\": \"/env/vault\"}):\n            config = Config.from_environment(vault_override=\"/override/vault\")\n            assert config.vault_path == Path(\"/override/vault\").absolute()\n\n    def test_env_var_used_when_no_override(self):\n        \"\"\"PROMPTKEEP_VAULT env var should be used when no override.\"\"\"\n        with patch.dict(os.environ, {\"PROMPTKEEP_VAULT\": \"/env/vault\"}, clear=True):\n            config = Config.from_environment()\n            assert config.vault_path == Path(\"/env/vault\").absolute()\n\n    def test_expands_user_in_vault_path(self):\n        \"\"\"Should expand ~ in vault path.\"\"\"\n        config = Config.from_environment(vault_override=\"~/my-vault\")\n        assert config.vault_path == Path.home() / \"my-vault\"\n\n    def test_uses_default_editor(self):\n        \"\"\"Should use vim when no EDITOR env var.\"\"\"\n        env_without_editor = {k: v for k, v in os.environ.items() if k != \"EDITOR\"}\n        with patch.dict(os.environ, env_without_editor, clear=True):\n            config = Config.from_environment()\n            assert config.editor == \"vim\"\n\n    def test_uses_editor_env_var(self):\n        \"\"\"Should use EDITOR env var when set.\"\"\"\n        with patch.dict(os.environ, {\"EDITOR\": \"nvim\"}):\n            config = Config.from_environment()\n            assert config.editor == \"nvim\"\n\n    def test_editor_override_takes_precedence(self):\n        \"\"\"Explicit editor_override should override env var.\"\"\"\n        with patch.dict(os.environ, {\"EDITOR\": \"nvim\"}):\n            config = Config.from_environment(editor_override=\"code\")\n            assert config.editor == \"code\"\n\n\nclass TestConfigValidateVault:\n    \"\"\"Tests for Config.validate_vault().\"\"\"\n\n    def test_raises_not_found_when_vault_missing(self, tmp_path):\n        \"\"\"Should raise VaultNotFoundError when vault doesn't exist.\"\"\"\n        config = Config(vault_path=tmp_path / \"nonexistent\", editor=\"vim\")\n        with pytest.raises(VaultNotFoundError) as exc_info:\n            config.validate_vault()\n        assert config.vault_path in exc_info.value.searched_paths\n\n    def test_raises_invalid_when_prompts_dir_missing(self, tmp_path):\n        \"\"\"Should raise VaultInvalidError when Prompts dir is missing.\"\"\"\n        vault = tmp_path / \"vault\"\n        vault.mkdir()\n        config = Config(vault_path=vault, editor=\"vim\")\n        with pytest.raises(VaultInvalidError) as exc_info:\n            config.validate_vault()\n        assert exc_info.value.vault_path == vault\n\n    def test_succeeds_when_vault_valid(self, tmp_path):\n        \"\"\"Should not raise when vault is valid.\"\"\"\n        vault = tmp_path / \"vault\"\n        prompts_dir = vault / \"Prompts\"\n        prompts_dir.mkdir(parents=True)\n        config = Config(vault_path=vault, editor=\"vim\")\n        config.validate_vault()  # Should not raise\n</code></pre> <ul> <li> Step 4.2: Run tests to verify they fail</li> </ul> <p>Run: <code>pytest tests/test_config.py -v</code> Expected: FAIL with \"ModuleNotFoundError: No module named 'promptkeep.config'\"</p> <ul> <li> Step 4.3: Implement config module</li> </ul> <pre><code># src/promptkeep/config.py\n\"\"\"Configuration management for PromptKeep.\n\nThis module provides centralized configuration with clear precedence:\n1. Explicit values passed to constructor or from_environment()\n2. Environment variables\n3. Default values\n\nThe Config class is immutable (frozen dataclass) to prevent accidental\nmodification after creation.\n\"\"\"\nimport os\nfrom dataclasses import dataclass\nfrom pathlib import Path\nfrom typing import Optional\n\nfrom promptkeep.constants import (\n    DEFAULT_EDITOR,\n    DEFAULT_VAULT_PATH,\n    ENV_EDITOR,\n    ENV_VAULT_PATH,\n    PROMPTS_DIR_NAME,\n)\nfrom promptkeep.exceptions import VaultInvalidError, VaultNotFoundError\n\n\n@dataclass(frozen=True)\nclass Config:\n    \"\"\"Immutable application configuration.\n\n    Attributes:\n        vault_path: Absolute path to the vault directory\n        editor: Command to open files for editing\n        prompts_dir_name: Name of the prompts subdirectory\n    \"\"\"\n\n    vault_path: Path\n    editor: str\n    prompts_dir_name: str = PROMPTS_DIR_NAME\n\n    @classmethod\n    def from_environment(\n        cls,\n        vault_override: Optional[str] = None,\n        editor_override: Optional[str] = None,\n    ) -&gt; \"Config\":\n        \"\"\"Load configuration from environment with optional overrides.\n\n        Args:\n            vault_override: Explicit vault path (highest precedence)\n            editor_override: Explicit editor command (highest precedence)\n\n        Returns:\n            Config instance with resolved values\n        \"\"\"\n        # Vault path resolution: override &gt; env var &gt; default\n        if vault_override:\n            vault_path = Path(vault_override).expanduser().absolute()\n        elif env_vault := os.environ.get(ENV_VAULT_PATH):\n            vault_path = Path(env_vault).expanduser().absolute()\n        else:\n            vault_path = Path(DEFAULT_VAULT_PATH).expanduser().absolute()\n\n        # Editor resolution: override &gt; env var &gt; default\n        editor = editor_override or os.environ.get(ENV_EDITOR, DEFAULT_EDITOR)\n\n        return cls(vault_path=vault_path, editor=editor)\n\n    @property\n    def prompts_dir(self) -&gt; Path:\n        \"\"\"Path to the prompts subdirectory.\"\"\"\n        return self.vault_path / self.prompts_dir_name\n\n    def validate_vault(self) -&gt; None:\n        \"\"\"Validate that the vault exists and is properly structured.\n\n        Raises:\n            VaultNotFoundError: If vault_path does not exist\n            VaultInvalidError: If vault exists but has no Prompts directory\n        \"\"\"\n        if not self.vault_path.exists():\n            raise VaultNotFoundError([self.vault_path])\n        if not self.prompts_dir.exists() or not self.prompts_dir.is_dir():\n            raise VaultInvalidError(self.vault_path)\n</code></pre> <ul> <li> Step 4.4: Run tests to verify they pass</li> </ul> <p>Run: <code>pytest tests/test_config.py -v</code> Expected: All tests PASS</p> <ul> <li> Step 4.5: Run linter</li> </ul> <p>Run: <code>ruff check src/promptkeep/config.py tests/test_config.py</code> Expected: No errors</p> <ul> <li> Step 4.6: Commit</li> </ul> <pre><code>git add src/promptkeep/config.py tests/test_config.py\ngit commit -m \"feat: add centralized configuration management\n\nConfig dataclass with:\n- Immutable (frozen) to prevent accidental modification\n- from_environment() with clear precedence rules\n- validate_vault() for existence and structure checks\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#task-5-create-service-implementations","title":"Task 5: Create Service Implementations","text":"<p>Files: - Create: <code>src/promptkeep/services.py</code> - Create: <code>tests/test_services.py</code></p> <ul> <li> Step 5.1: Write service tests</li> </ul> <pre><code># tests/test_services.py\n\"\"\"Tests for service implementations.\"\"\"\nimport subprocess\nfrom pathlib import Path\nfrom unittest.mock import MagicMock, patch\n\nimport pytest\n\nfrom promptkeep.exceptions import (\n    EditorError,\n    EditorNotFoundError,\n    SelectorNotFoundError,\n)\nfrom promptkeep.services import FzfPromptSelector, SystemClipboard, SystemEditor\n\n\nclass TestSystemClipboard:\n    \"\"\"Tests for SystemClipboard service.\"\"\"\n\n    def test_copy_calls_pyperclip(self):\n        \"\"\"Should delegate to pyperclip.copy().\"\"\"\n        with patch(\"promptkeep.services.pyperclip\") as mock_pyperclip:\n            clipboard = SystemClipboard()\n            clipboard.copy(\"test text\")\n            mock_pyperclip.copy.assert_called_once_with(\"test text\")\n\n\nclass TestSystemEditor:\n    \"\"\"Tests for SystemEditor service.\"\"\"\n\n    def test_open_calls_subprocess(self, tmp_path):\n        \"\"\"Should call subprocess.run with editor command.\"\"\"\n        test_file = tmp_path / \"test.txt\"\n        test_file.touch()\n\n        with patch(\"promptkeep.services.subprocess.run\") as mock_run:\n            editor = SystemEditor(\"vim\")\n            result = editor.open(test_file)\n\n            assert result is True\n            mock_run.assert_called_once()\n            call_args = mock_run.call_args[0][0]\n            assert call_args[0] == \"vim\"\n            assert str(test_file) in call_args\n\n    def test_open_handles_editor_with_args(self, tmp_path):\n        \"\"\"Should handle editor commands with arguments like 'code --wait'.\"\"\"\n        test_file = tmp_path / \"test.txt\"\n        test_file.touch()\n\n        with patch(\"promptkeep.services.subprocess.run\") as mock_run:\n            editor = SystemEditor(\"code --wait\")\n            editor.open(test_file)\n\n            call_args = mock_run.call_args[0][0]\n            assert call_args[0] == \"code\"\n            assert \"--wait\" in call_args\n\n    def test_open_raises_not_found_error(self, tmp_path):\n        \"\"\"Should raise EditorNotFoundError when editor not found.\"\"\"\n        test_file = tmp_path / \"test.txt\"\n        test_file.touch()\n\n        with patch(\n            \"promptkeep.services.subprocess.run\",\n            side_effect=FileNotFoundError(),\n        ):\n            editor = SystemEditor(\"nonexistent-editor\")\n            with pytest.raises(EditorNotFoundError) as exc_info:\n                editor.open(test_file)\n            assert exc_info.value.editor == \"nonexistent-editor\"\n\n    def test_open_raises_editor_error_on_failure(self, tmp_path):\n        \"\"\"Should raise EditorError when editor exits with error.\"\"\"\n        test_file = tmp_path / \"test.txt\"\n        test_file.touch()\n\n        with patch(\n            \"promptkeep.services.subprocess.run\",\n            side_effect=subprocess.CalledProcessError(1, \"vim\"),\n        ):\n            editor = SystemEditor(\"vim\")\n            with pytest.raises(EditorError):\n                editor.open(test_file)\n\n\nclass TestFzfPromptSelector:\n    \"\"\"Tests for FzfPromptSelector service.\"\"\"\n\n    def test_select_returns_path_on_selection(self, tmp_path):\n        \"\"\"Should return selected path.\"\"\"\n        file1 = tmp_path / \"prompt1.md\"\n        file2 = tmp_path / \"prompt2.md\"\n        file1.touch()\n        file2.touch()\n\n        with patch(\"promptkeep.services.subprocess.check_output\") as mock_output:\n            mock_output.return_value = str(file1).encode()\n            selector = FzfPromptSelector(\"preview script\")\n            result = selector.select([file1, file2], \"Select: \")\n\n            assert result == file1\n\n    def test_select_returns_none_on_cancel(self, tmp_path):\n        \"\"\"Should return None when user cancels.\"\"\"\n        file1 = tmp_path / \"prompt1.md\"\n        file1.touch()\n\n        with patch(\n            \"promptkeep.services.subprocess.check_output\",\n            side_effect=subprocess.CalledProcessError(1, \"fzf\"),\n        ):\n            selector = FzfPromptSelector(\"preview script\")\n            result = selector.select([file1], \"Select: \")\n            assert result is None\n\n    def test_select_returns_none_for_empty_list(self):\n        \"\"\"Should return None for empty items list.\"\"\"\n        selector = FzfPromptSelector(\"preview script\")\n        result = selector.select([], \"Select: \")\n        assert result is None\n\n    def test_select_raises_not_found_error(self, tmp_path):\n        \"\"\"Should raise SelectorNotFoundError when fzf not installed.\"\"\"\n        file1 = tmp_path / \"prompt1.md\"\n        file1.touch()\n\n        with patch(\n            \"promptkeep.services.subprocess.check_output\",\n            side_effect=FileNotFoundError(),\n        ):\n            selector = FzfPromptSelector(\"preview script\")\n            with pytest.raises(SelectorNotFoundError):\n                selector.select([file1], \"Select: \")\n\n    def test_select_passes_preview_script(self, tmp_path):\n        \"\"\"Should pass preview script to fzf.\"\"\"\n        file1 = tmp_path / \"prompt1.md\"\n        file1.touch()\n\n        with patch(\"promptkeep.services.subprocess.check_output\") as mock_output:\n            mock_output.return_value = str(file1).encode()\n            selector = FzfPromptSelector(\"my preview script {}\")\n            selector.select([file1], \"Select: \")\n\n            call_args = mock_output.call_args[0][0]\n            assert \"--preview\" in call_args\n            preview_idx = call_args.index(\"--preview\")\n            assert call_args[preview_idx + 1] == \"my preview script {}\"\n</code></pre> <ul> <li> Step 5.2: Run tests to verify they fail</li> </ul> <p>Run: <code>pytest tests/test_services.py -v</code> Expected: FAIL with \"ModuleNotFoundError: No module named 'promptkeep.services'\"</p> <ul> <li> Step 5.3: Implement services module</li> </ul> <pre><code># src/promptkeep/services.py\n\"\"\"Concrete service implementations for PromptKeep.\n\nThis module provides production implementations of the service protocols.\nEach service wraps an external dependency (pyperclip, subprocess, fzf)\nand converts errors to PromptKeep exceptions.\n\"\"\"\nimport shlex\nimport subprocess\nfrom pathlib import Path\nfrom typing import List, Optional\n\nimport pyperclip\n\nfrom promptkeep.exceptions import (\n    EditorError,\n    EditorNotFoundError,\n    SelectorNotFoundError,\n)\n\n\nclass SystemClipboard:\n    \"\"\"Clipboard implementation using pyperclip.\"\"\"\n\n    def copy(self, text: str) -&gt; None:\n        \"\"\"Copy text to the system clipboard.\"\"\"\n        pyperclip.copy(text)\n\n\nclass SystemEditor:\n    \"\"\"Editor implementation using subprocess.\n\n    Handles editor commands that may include arguments (e.g., 'code --wait')\n    by using shlex.split() for safe parsing.\n    \"\"\"\n\n    def __init__(self, editor_command: str):\n        \"\"\"Initialize with the editor command.\n\n        Args:\n            editor_command: Editor command, may include args (e.g., 'vim', 'code --wait')\n        \"\"\"\n        self.editor_command = editor_command\n\n    def open(self, file_path: Path) -&gt; bool:\n        \"\"\"Open a file in the editor.\n\n        Args:\n            file_path: Path to the file to edit\n\n        Returns:\n            True if editor exited successfully\n\n        Raises:\n            EditorNotFoundError: If the editor binary is not found\n            EditorError: If the editor exits with an error or command is malformed\n        \"\"\"\n        try:\n            editor_parts = shlex.split(self.editor_command)\n            editor_parts.append(str(file_path))\n            subprocess.run(editor_parts, check=True)\n            return True\n        except FileNotFoundError:\n            raise EditorNotFoundError(self.editor_command)\n        except subprocess.CalledProcessError as e:\n            raise EditorError(f\"Editor exited with code {e.returncode}\")\n        except ValueError as e:\n            raise EditorError(f\"Invalid editor command: {e}\")\n\n\nclass FzfPromptSelector:\n    \"\"\"Prompt selector using fzf.\n\n    The preview_script is passed directly to fzf's --preview flag.\n    It should contain {} as a placeholder where fzf will substitute\n    the currently highlighted filename (this is standard fzf behavior).\n    \"\"\"\n\n    def __init__(self, preview_script: str):\n        \"\"\"Initialize with the preview script.\n\n        Args:\n            preview_script: Script for fzf preview, should contain {} placeholder\n        \"\"\"\n        self.preview_script = preview_script\n\n    def select(self, items: List[Path], prompt: str) -&gt; Optional[Path]:\n        \"\"\"Show interactive selection UI.\n\n        Args:\n            items: List of file paths to select from\n            prompt: Prompt text to display\n\n        Returns:\n            Selected path, or None if user cancels or list is empty\n\n        Raises:\n            SelectorNotFoundError: If fzf is not installed\n        \"\"\"\n        if not items:\n            return None\n\n        try:\n            result = subprocess.check_output(\n                [\"fzf\", \"--prompt\", prompt, \"--preview\", self.preview_script],\n                input=\"\\n\".join(str(f) for f in items).encode(),\n            )\n            return Path(result.decode().strip())\n        except subprocess.CalledProcessError:\n            # User cancelled selection (Ctrl+C or Esc)\n            return None\n        except FileNotFoundError:\n            raise SelectorNotFoundError()\n</code></pre> <ul> <li> Step 5.4: Run tests to verify they pass</li> </ul> <p>Run: <code>pytest tests/test_services.py -v</code> Expected: All tests PASS</p> <ul> <li> Step 5.5: Run linter</li> </ul> <p>Run: <code>ruff check src/promptkeep/services.py tests/test_services.py</code> Expected: No errors</p> <ul> <li> Step 5.6: Commit</li> </ul> <pre><code>git add src/promptkeep/services.py tests/test_services.py\ngit commit -m \"feat: add service implementations\n\nConcrete implementations of service protocols:\n- SystemClipboard wrapping pyperclip\n- SystemEditor with safe command parsing via shlex\n- FzfPromptSelector for interactive selection\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#task-6-create-application-context","title":"Task 6: Create Application Context","text":"<p>Files: - Create: <code>src/promptkeep/context.py</code> - Create: <code>tests/test_context.py</code></p> <ul> <li> Step 6.1: Write context tests</li> </ul> <pre><code># tests/test_context.py\n\"\"\"Tests for application context.\"\"\"\nfrom pathlib import Path\nfrom unittest.mock import MagicMock\n\nimport pytest\n\nfrom promptkeep.config import Config\nfrom promptkeep.context import AppContext\n\n\nclass TestAppContext:\n    \"\"\"Tests for AppContext.\"\"\"\n\n    def test_create_default_returns_context(self, tmp_path):\n        \"\"\"Should create context with all services.\"\"\"\n        vault = tmp_path / \"vault\"\n        prompts = vault / \"Prompts\"\n        prompts.mkdir(parents=True)\n\n        config = Config(vault_path=vault, editor=\"vim\")\n        ctx = AppContext.create_default(config)\n\n        assert ctx.config == config\n        assert ctx.clipboard is not None\n        assert ctx.editor is not None\n        assert ctx.selector is not None\n        assert ctx.console is not None\n        assert ctx.repository is not None\n\n    def test_context_uses_config_editor(self, tmp_path):\n        \"\"\"Editor service should use editor from config.\"\"\"\n        vault = tmp_path / \"vault\"\n        prompts = vault / \"Prompts\"\n        prompts.mkdir(parents=True)\n\n        config = Config(vault_path=vault, editor=\"nvim\")\n        ctx = AppContext.create_default(config)\n\n        # The editor service should have the correct command\n        assert ctx.editor.editor_command == \"nvim\"\n\n    def test_context_uses_config_vault_path(self, tmp_path):\n        \"\"\"Repository should use vault_path from config.\"\"\"\n        vault = tmp_path / \"vault\"\n        prompts = vault / \"Prompts\"\n        prompts.mkdir(parents=True)\n\n        config = Config(vault_path=vault, editor=\"vim\")\n        ctx = AppContext.create_default(config)\n\n        assert ctx.repository.vault_path == vault\n</code></pre> <ul> <li> Step 6.2: Run tests to verify they fail</li> </ul> <p>Run: <code>pytest tests/test_context.py -v</code> Expected: FAIL with \"ModuleNotFoundError: No module named 'promptkeep.context'\"</p> <ul> <li> Step 6.3: Implement context module</li> </ul> <pre><code># src/promptkeep/context.py\n\"\"\"Application context for PromptKeep.\n\nThis module provides the composition root where all dependencies are\nwired together. The AppContext holds all services and is passed to\ncommands, enabling dependency injection and easy testing.\n\"\"\"\nfrom dataclasses import dataclass\n\nfrom rich.console import Console\n\nfrom promptkeep.config import Config\nfrom promptkeep.constants import FZF_PREVIEW_SCRIPT\nfrom promptkeep.protocols import (\n    ClipboardService,\n    EditorService,\n    PromptRepositoryProtocol,\n    PromptSelector,\n)\nfrom promptkeep.repository import PromptRepository\nfrom promptkeep.services import FzfPromptSelector, SystemClipboard, SystemEditor\n\n\n@dataclass\nclass AppContext:\n    \"\"\"Container for all application dependencies.\n\n    This is the composition root - the single place where concrete\n    implementations are wired together. Commands receive this context\n    and use the abstract interfaces, making them easy to test with mocks.\n    \"\"\"\n\n    config: Config\n    clipboard: ClipboardService\n    editor: EditorService\n    selector: PromptSelector\n    console: Console\n    repository: PromptRepositoryProtocol\n\n    @classmethod\n    def create_default(cls, config: Config) -&gt; \"AppContext\":\n        \"\"\"Create context with default production implementations.\n\n        Args:\n            config: Application configuration\n\n        Returns:\n            AppContext with all services wired up\n        \"\"\"\n        return cls(\n            config=config,\n            clipboard=SystemClipboard(),\n            editor=SystemEditor(config.editor),\n            selector=FzfPromptSelector(FZF_PREVIEW_SCRIPT),\n            console=Console(),\n            repository=PromptRepository(config.vault_path),\n        )\n</code></pre> <ul> <li> Step 6.4: Run tests to verify they pass</li> </ul> <p>Run: <code>pytest tests/test_context.py -v</code> Expected: All tests PASS</p> <ul> <li> Step 6.5: Run linter</li> </ul> <p>Run: <code>ruff check src/promptkeep/context.py tests/test_context.py</code> Expected: No errors</p> <ul> <li> Step 6.6: Commit</li> </ul> <pre><code>git add src/promptkeep/context.py tests/test_context.py\ngit commit -m \"feat: add AppContext composition root\n\nAppContext wires together all services:\n- Config for configuration\n- Clipboard, Editor, Selector services\n- Console for output\n- Repository for storage\n\ncreate_default() provides production implementations.\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#task-7-refactor-cli-to-use-appcontext","title":"Task 7: Refactor CLI to Use AppContext","text":"<p>Scope: Refactor <code>pick</code>, <code>edit</code>, and <code>add</code> commands to use AppContext and services.</p> <p>Note on <code>init</code> command: The <code>init</code> command does NOT need refactoring because it: - Only creates directories (no editor, selector, or clipboard services needed) - Doesn't require vault validation (it creates the vault) - Already uses Rich console directly, which is fine for simple output</p> <p>The error handling decorator will be applied to commands that could throw PromptKeepError.</p> <p>Files: - Modify: <code>src/promptkeep/cli.py</code> - Modify: <code>tests/test_cli.py</code></p> <ul> <li> Step 7.1: Fix test_cli.py linting issues first</li> </ul> <p>Edit <code>tests/test_cli.py</code> to fix imports:</p> <pre><code># At the top of tests/test_cli.py, replace ALL imports with:\n\"\"\"\nTests for the CLI module.\n\nThese tests verify the command-line interface functionality including:\n- Vault initialization\n- Prompt creation and editing\n- Prompt selection and clipboard operations\n\nTests use mocking to avoid filesystem changes and external dependencies,\nand prevent side effects on the actual filesystem.\n\"\"\"\nimport os\nfrom pathlib import Path\nfrom tempfile import TemporaryDirectory\nfrom unittest.mock import MagicMock, patch\n\nimport pytest\nimport typer\n\nfrom promptkeep import cli\nfrom promptkeep.utils import extract_prompt_content, sanitize_filename\n</code></pre> <p>Removed imports: - <code>shutil</code> (unused) - <code>find_vault_path</code> (moved to Config) - <code>copy_to_clipboard</code> (moved to SystemClipboard) - <code>open_editor</code> (moved to SystemEditor)</p> <ul> <li> Step 7.2: Run linter on test file</li> </ul> <p>Run: <code>ruff check tests/test_cli.py</code> Expected: No errors (or only errors related to cli.py changes we haven't made yet)</p> <ul> <li> Step 7.3: Update cli.py with new imports and helpers</li> </ul> <p>Replace the imports and add helpers at the top of <code>src/promptkeep/cli.py</code>:</p> <pre><code>\"\"\"\nMain CLI module for PromptKeep.\n\nThis module implements the command-line interface for PromptKeep, providing commands\nfor initializing a prompt vault, adding new prompts, and selecting existing prompts.\nIt uses Typer for CLI argument parsing and Rich for terminal output formatting.\n\"\"\"\nimport shutil\nfrom datetime import datetime\nfrom functools import wraps\nfrom pathlib import Path\nfrom typing import Callable, List, Optional, TypeVar\n\nimport typer\nfrom rich.console import Console\nfrom rich.panel import Panel\nfrom rich.progress import Progress, SpinnerColumn, TextColumn\n\nfrom promptkeep.config import Config\nfrom promptkeep.constants import FZF_PREVIEW_SCRIPT, PROMPTS_DIR_NAME\nfrom promptkeep.context import AppContext\nfrom promptkeep.exceptions import (\n    EditorError,\n    EditorNotFoundError,\n    PromptKeepError,\n    SelectorNotFoundError,\n    VaultInvalidError,\n    VaultNotFoundError,\n)\nfrom promptkeep.models import Prompt\nfrom promptkeep.repository import PromptRepository\nfrom promptkeep.utils import extract_prompt_content, sanitize_filename\n\n# Initialize the Typer app with a help message\napp = typer.Typer(help=\"PromptKeep - A CLI tool for managing and accessing your AI prompts\")\nconsole = Console()\n\nF = TypeVar(\"F\", bound=Callable[..., None])\n\n\ndef handle_errors(func: F) -&gt; F:\n    \"\"\"Decorator to convert PromptKeepError to user-friendly CLI output.\n\n    Uses the module-level console for error output.\n    \"\"\"\n    @wraps(func)\n    def wrapper(*args, **kwargs):\n        try:\n            return func(*args, **kwargs)\n        except VaultNotFoundError:\n            console.print(\n                Panel.fit(\n                    \"[red]Error: No vault found.[/]\\n\"\n                    \"Use 'promptkeep init' to create a vault or specify --vault.\",\n                    title=\"Error\",\n                    border_style=\"red\",\n                )\n            )\n            raise typer.Exit(1)\n        except VaultInvalidError as e:\n            console.print(\n                Panel.fit(\n                    f\"[red]Error: Invalid vault at {e.vault_path}[/]\\n\"\n                    \"Make sure this is a valid prompt vault with a Prompts directory.\",\n                    title=\"Error\",\n                    border_style=\"red\",\n                )\n            )\n            raise typer.Exit(1)\n        except EditorNotFoundError as e:\n            console.print(\n                Panel.fit(\n                    f\"[red]Error: Editor '{e.editor}' not found.[/]\\n\"\n                    \"Please set the EDITOR environment variable to your preferred editor.\",\n                    title=\"Error\",\n                    border_style=\"red\",\n                )\n            )\n            raise typer.Exit(1)\n        except EditorError as e:\n            console.print(\n                Panel.fit(\n                    f\"[red]Error: {e}[/]\",\n                    title=\"Error\",\n                    border_style=\"red\",\n                )\n            )\n            raise typer.Exit(1)\n        except SelectorNotFoundError:\n            console.print(\n                Panel.fit(\n                    \"[red]Error: fzf not found.[/]\\n\"\n                    \"Please install fzf to use this command.\",\n                    title=\"Error\",\n                    border_style=\"red\",\n                )\n            )\n            raise typer.Exit(1)\n\n    return wrapper  # type: ignore[return-value]\n\n\ndef get_context(vault_path: Optional[str] = None) -&gt; AppContext:\n    \"\"\"Get or create AppContext with optional vault override.\n\n    This helper is called by commands that need the full context.\n    Vault validation is the caller's responsibility via config.validate_vault().\n    \"\"\"\n    config = Config.from_environment(vault_override=vault_path)\n    return AppContext.create_default(config)\n</code></pre> <ul> <li> Step 7.4: Update pick_command to use AppContext</li> </ul> <p>Replace the <code>pick_command</code> function in <code>src/promptkeep/cli.py</code>:</p> <pre><code>@app.command(\"pick\")\n@handle_errors\ndef pick_command(\n    vault_path: Optional[str] = typer.Option(\n        None,\n        \"--vault\", \"-v\",\n        help=\"Path to the prompt vault (defaults to ~/PromptVault or PROMPTKEEP_VAULT env var)\",\n    ),\n    tags: Optional[List[str]] = typer.Option(\n        None,\n        \"--tag\", \"-t\",\n        help=\"Filter prompts by tag (can be specified multiple times)\",\n    ),\n) -&gt; None:\n    \"\"\"Select a prompt and copy its content to clipboard.\n\n    This command provides an interactive selection interface using fzf.\n    Use fuzzy search to filter prompts, then press Enter to copy the\n    selected prompt's content to your clipboard.\n    \"\"\"\n    ctx = get_context(vault_path)\n    ctx.config.validate_vault()\n\n    prompt_files = ctx.repository.get_file_paths(tags=list(tags) if tags else None)\n\n    if not prompt_files:\n        if tags:\n            tag_list = \", \".join(f\"'{tag}'\" for tag in tags)\n            ctx.console.print(\n                Panel.fit(\n                    f\"[yellow]No prompts found with tags: {tag_list}[/]\",\n                    title=\"Warning\",\n                    border_style=\"yellow\",\n                )\n            )\n        else:\n            ctx.console.print(\n                Panel.fit(\n                    \"[yellow]No prompts found in the vault.[/]\\n\"\n                    \"Use 'promptkeep add' to create a new prompt.\",\n                    title=\"Warning\",\n                    border_style=\"yellow\",\n                )\n            )\n        raise typer.Exit(1)\n\n    selected_file = ctx.selector.select(prompt_files, \"Select a prompt: \")\n    if not selected_file:\n        raise typer.Exit(0)  # User cancelled\n\n    content = selected_file.read_text()\n    prompt_content = extract_prompt_content(content)\n    ctx.clipboard.copy(prompt_content)\n\n    ctx.console.print(\n        Panel.fit(\n            \"Prompt copied to clipboard\",\n            title=\"Success\",\n            border_style=\"green\",\n        )\n    )\n</code></pre> <ul> <li> Step 7.5: Update edit_command to use AppContext</li> </ul> <p>Replace the <code>edit_command</code> function in <code>src/promptkeep/cli.py</code>:</p> <pre><code>@app.command(\"edit\")\n@handle_errors\ndef edit_command(\n    vault_path: Optional[str] = typer.Option(\n        None,\n        \"--vault\", \"-v\",\n        help=\"Path to the prompt vault (defaults to ~/PromptVault or PROMPTKEEP_VAULT env var)\",\n    ),\n    tags: Optional[List[str]] = typer.Option(\n        None,\n        \"--tag\", \"-t\",\n        help=\"Filter prompts by tag (can be specified multiple times)\",\n    ),\n) -&gt; None:\n    \"\"\"Edit an existing prompt in your vault.\n\n    This command provides an interactive selection interface using fzf.\n    After selecting a prompt, it opens in your configured editor.\n    \"\"\"\n    ctx = get_context(vault_path)\n    ctx.config.validate_vault()\n\n    prompt_files = ctx.repository.get_file_paths(tags=list(tags) if tags else None)\n\n    if not prompt_files:\n        if tags:\n            tag_list = \", \".join(f\"'{tag}'\" for tag in tags)\n            ctx.console.print(\n                Panel.fit(\n                    f\"[yellow]No prompts found with tags: {tag_list}[/]\",\n                    title=\"Warning\",\n                    border_style=\"yellow\",\n                )\n            )\n        else:\n            ctx.console.print(\n                Panel.fit(\n                    \"[yellow]No prompts found in the vault.[/]\\n\"\n                    \"Use 'promptkeep add' to create a new prompt.\",\n                    title=\"Warning\",\n                    border_style=\"yellow\",\n                )\n            )\n        raise typer.Exit(1)\n\n    selected_file = ctx.selector.select(prompt_files, \"Select a prompt to edit: \")\n    if not selected_file:\n        raise typer.Exit(0)  # User cancelled\n\n    ctx.console.print(\n        Panel.fit(\n            f\"Opening editor for you to edit the prompt.\\n\"\n            f\"File: [bold blue]{selected_file}[/]\",\n            title=\"Editing Prompt\",\n            border_style=\"blue\",\n        )\n    )\n\n    ctx.editor.open(selected_file)\n\n    ctx.console.print(\n        Panel.fit(\n            \"Prompt updated successfully\",\n            title=\"Success\",\n            border_style=\"green\",\n        )\n    )\n</code></pre> <ul> <li> Step 7.6: Update add_command to use services</li> </ul> <p>Update <code>add_command</code> to use services from context for editor operations:</p> <pre><code>@app.command(\"add\")\n@handle_errors\ndef add_command(\n    title: str = typer.Option(\n        ...,\n        \"--title\", \"-t\",\n        help=\"Title of the prompt\",\n        prompt=\"Enter a title for your prompt\",\n    ),\n    description: str = typer.Option(\n        \"\",\n        \"--description\", \"-d\",\n        help=\"Description of the prompt\",\n        prompt=\"Enter a description (optional)\",\n    ),\n    tags: List[str] = typer.Option(\n        [],\n        \"--tag\",\n        help=\"Tags for the prompt (can be specified multiple times)\",\n    ),\n    tags_prompt_str: Optional[str] = typer.Option(\n        \"\",\n        prompt=\"Enter tags separated by commas (optional)\",\n        help=\"Internal use for prompt input\",\n        hidden=True,\n        show_default=False,\n    ),\n    vault_path: Optional[str] = typer.Option(\n        None,\n        \"--vault\", \"-v\",\n        help=\"Path to the prompt vault (defaults to ~/PromptVault or PROMPTKEEP_VAULT env var)\",\n    ),\n) -&gt; None:\n    \"\"\"Add a new prompt to your vault.\n\n    This command creates a new markdown file with YAML front matter,\n    then opens your editor to write the prompt content.\n    \"\"\"\n    ctx = get_context(vault_path)\n    ctx.config.validate_vault()\n\n    prompts_dir = ctx.config.prompts_dir\n\n    # Combine tags from --tag flags and the prompt string\n    processed_tags = tags[:]\n    if tags_prompt_str and isinstance(tags_prompt_str, str):\n        prompt_tags = [tag.strip() for tag in tags_prompt_str.split(\",\") if tag.strip()]\n        processed_tags.extend(prompt_tags)\n    processed_tags = sorted(list(set(processed_tags)))\n\n    # Create filename from title and timestamp\n    filename = sanitize_filename(title)\n    timestamp = datetime.now().strftime(\"%Y%m%d-%H%M%S\")\n    filename = f\"{filename}-{timestamp}.md\"\n    prompt_path = prompts_dir / filename\n\n    # Check for existing similar prompts\n    existing_files = list(prompts_dir.glob(f\"{sanitize_filename(title)}-*.md\"))\n    if existing_files:\n        ctx.console.print(\n            Panel.fit(\n                f\"[yellow]Warning: Similar prompts already exist:[/]\\n\"\n                f\"{chr(10).join(f'- {f.name}' for f in existing_files)}\",\n                title=\"Warning\",\n                border_style=\"yellow\",\n            )\n        )\n        if not typer.confirm(\"Do you want to continue?\"):\n            raise typer.Exit(0)\n\n    # Create the prompt using the Prompt model\n    prompt = Prompt(\n        title=title,\n        description=description,\n        tags=processed_tags,\n        content=\"\"\n    )\n    prompt_path.write_text(prompt.to_markdown())\n\n    ctx.console.print(\n        Panel.fit(\n            f\"Opening editor for you to write your prompt content.\\n\"\n            f\"File will be saved at: [bold blue]{prompt_path}[/]\",\n            title=\"Creating Prompt\",\n            border_style=\"blue\",\n        )\n    )\n\n    try:\n        ctx.editor.open(prompt_path)\n    except PromptKeepError:\n        prompt_path.unlink()\n        raise\n\n    if prompt_path.exists():\n        ctx.console.print(\n            Panel.fit(\n                f\"Prompt created successfully at: [bold blue]{prompt_path}[/]\",\n                title=\"Success\",\n                border_style=\"green\",\n            )\n        )\n    else:\n        ctx.console.print(\n            Panel.fit(\n                \"[yellow]Note: Prompt file was not saved.[/]\",\n                title=\"Warning\",\n                border_style=\"yellow\",\n            )\n        )\n</code></pre> <ul> <li> Step 7.7: Remove _FZF_PREVIEW_SCRIPT from cli.py</li> </ul> <p>Delete the <code>_FZF_PREVIEW_SCRIPT</code> variable (lines ~34-53 in original) since it's now in constants.py.</p> <ul> <li> Step 7.8: Remove old subprocess/fzf code from pick and edit</li> </ul> <p>The old <code>subprocess.check_output</code> calls for fzf should be removed - they're now handled by <code>FzfPromptSelector</code>.</p> <ul> <li> Step 7.9: Run all tests</li> </ul> <p>Run: <code>pytest -v</code> Expected: Most tests pass (some CLI tests may need updates)</p> <ul> <li> Step 7.10: Update CLI tests for new patterns</li> </ul> <p>Update tests that were testing the old subprocess patterns to work with the new service-based approach. Tests should mock the services rather than subprocess.</p> <ul> <li> Step 7.11: Run linter</li> </ul> <p>Run: <code>ruff check src/promptkeep/cli.py tests/test_cli.py</code> Expected: No errors</p> <ul> <li> Step 7.12: Run full test suite</li> </ul> <p>Run: <code>pytest -v</code> Expected: All tests PASS</p> <ul> <li> Step 7.13: Commit</li> </ul> <pre><code>git add src/promptkeep/cli.py tests/test_cli.py\ngit commit -m \"refactor: CLI to use AppContext and services\n\n- Add handle_errors decorator for exception handling\n- Add get_context() helper for context creation\n- Refactor pick, edit, add commands to use AppContext\n- Remove inline subprocess calls (now in services)\n- Fix test imports and update for new patterns\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#task-8-clean-up-utils-module","title":"Task 8: Clean Up Utils Module","text":"<p>Files: - Modify: <code>src/promptkeep/utils.py</code> - Modify: <code>tests/test_utils.py</code></p> <ul> <li> Step 8.1: Remove functions that moved to services</li> </ul> <p>Remove from <code>src/promptkeep/utils.py</code>: - <code>open_editor()</code> function (moved to SystemEditor) - <code>copy_to_clipboard()</code> function (moved to SystemClipboard) - <code>find_vault_path()</code> function (replaced by Config) - <code>validate_vault_path()</code> function (replaced by Config.validate_vault)</p> <p>Keep: - <code>sanitize_filename()</code> - <code>extract_prompt_content()</code></p> <ul> <li> Step 8.2: Update utils imports</li> </ul> <p>Clean up imports in utils.py to only include what's needed:</p> <pre><code>\"\"\"\nUtility functions for PromptKeep.\n\nThis module provides core utility functions used throughout the application.\n\"\"\"\nimport re\nfrom pathlib import Path\n\nfrom promptkeep.constants import MAX_FILENAME_LENGTH, YAML_DELIMITER\n</code></pre> <ul> <li> Step 8.3: Update test_utils.py</li> </ul> <p>Remove tests for deleted functions. Keep tests for <code>sanitize_filename</code> and <code>extract_prompt_content</code>.</p> <ul> <li> Step 8.4: Run tests</li> </ul> <p>Run: <code>pytest tests/test_utils.py -v</code> Expected: All tests PASS</p> <ul> <li> Step 8.5: Run linter</li> </ul> <p>Run: <code>ruff check src/promptkeep/utils.py tests/test_utils.py</code> Expected: No errors</p> <ul> <li> Step 8.6: Commit</li> </ul> <pre><code>git add src/promptkeep/utils.py tests/test_utils.py\ngit commit -m \"refactor: slim down utils module\n\nRemove functions that moved to other modules:\n- open_editor -&gt; SystemEditor service\n- copy_to_clipboard -&gt; SystemClipboard service\n- find_vault_path, validate_vault_path -&gt; Config class\n\nKeep pure utilities: sanitize_filename, extract_prompt_content\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#task-9-final-verification","title":"Task 9: Final Verification","text":"<p>Files: - All modified files</p> <ul> <li> Step 9.1: Run full test suite with coverage</li> </ul> <p>Run: <code>pytest --cov=promptkeep --cov-report=term-missing</code> Expected: All tests pass, coverage &gt; 90%</p> <ul> <li> Step 9.2: Run linter on entire codebase</li> </ul> <p>Run: <code>ruff check src/ tests/</code> Expected: No errors</p> <ul> <li> Step 9.3: Run type checker</li> </ul> <p>Run: <code>mypy src/promptkeep/</code> Expected: No errors (or only minor issues with third-party libraries)</p> <ul> <li> Step 9.4: Manual smoke test</li> </ul> <pre><code># Test init\npromptkeep init /tmp/test-vault\n\n# Test add (will open editor - just save and exit)\npromptkeep add --title \"Test\" --vault /tmp/test-vault\n\n# Test pick (requires fzf)\npromptkeep pick --vault /tmp/test-vault\n\n# Cleanup\nrm -rf /tmp/test-vault\n</code></pre> <ul> <li> Step 9.5: Final commit</li> </ul> <pre><code>git add -A\ngit commit -m \"chore: final cleanup and verification\n\nAll tests passing with &gt;90% coverage.\nNo linting errors.\"\n</code></pre>"},{"location":"superpowers/plans/2026-03-23-architecture-improvements/#summary","title":"Summary","text":"Task Description Estimated Time 1 Exception hierarchy 15 min 2 Protocol interfaces 10 min 3 Constants update 5 min 4 Configuration module 20 min 5 Service implementations 25 min 6 Application context 15 min 7 CLI refactoring 45 min 8 Utils cleanup 15 min 9 Final verification 15 min <p>Total estimated time: ~2.5-3 hours</p>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/","title":"PromptKeep Architecture Improvements","text":"<p>Date: 2026-03-23 Status: Approved Goal: Improve code architecture to demonstrate backend/systems engineering best practices</p>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#overview","title":"Overview","text":"<p>Refactor PromptKeep to showcase professional software design patterns while maintaining the existing functionality. The focus is on configuration management, dependency injection, and code polish.</p>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#current-state","title":"Current State","text":"<ul> <li>Well-structured CLI with clean separation (models, repository, utils, cli, constants)</li> <li>91% test coverage with 81 tests</li> <li>Minor issues: 2 linting errors, some scattered configuration logic</li> <li>Direct dependencies on external libraries (pyperclip, subprocess) make testing require patching</li> </ul>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#design-goals","title":"Design Goals","text":"<ol> <li>Centralize configuration management</li> <li>Introduce dependency injection for testability</li> <li>Define clear interfaces using Protocol classes</li> <li>Create a custom exception hierarchy</li> <li>Fix all linting issues and ensure comprehensive type hints</li> </ol>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#architecture","title":"Architecture","text":""},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#new-modules","title":"New Modules","text":""},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#configpy-configuration-management","title":"<code>config.py</code> - Configuration Management","text":"<pre><code>from dataclasses import dataclass\nfrom pathlib import Path\nfrom typing import Optional\nimport os\n\nfrom promptkeep.constants import (\n    DEFAULT_VAULT_PATH,\n    DEFAULT_EDITOR,\n    ENV_VAULT_PATH,\n    ENV_EDITOR,\n    PROMPTS_DIR_NAME,\n)\n\n@dataclass(frozen=True)\nclass Config:\n    \"\"\"Immutable application configuration.\n\n    Configuration precedence (highest to lowest):\n    1. Explicit values passed to constructor\n    2. Environment variables\n    3. Default values\n\n    Note: This class handles configuration resolution only. It does NOT validate\n    that the vault exists or is properly structured. Vault validation happens\n    at the command level, which can then raise VaultNotFoundError or\n    VaultInvalidError as appropriate.\n    \"\"\"\n    vault_path: Path\n    editor: str\n    prompts_dir_name: str = PROMPTS_DIR_NAME\n\n    @classmethod\n    def from_environment(\n        cls,\n        vault_override: Optional[str] = None,\n        editor_override: Optional[str] = None,\n    ) -&gt; \"Config\":\n        \"\"\"Load configuration from environment with optional overrides.\n\n        This method resolves paths but does not validate vault existence.\n        \"\"\"\n        # Vault path resolution\n        if vault_override:\n            vault_path = Path(vault_override).expanduser().absolute()\n        elif env_vault := os.environ.get(ENV_VAULT_PATH):\n            vault_path = Path(env_vault).expanduser().absolute()\n        else:\n            vault_path = Path(DEFAULT_VAULT_PATH).expanduser().absolute()\n\n        # Editor resolution\n        editor = editor_override or os.environ.get(ENV_EDITOR, DEFAULT_EDITOR)\n\n        return cls(vault_path=vault_path, editor=editor)\n\n    @property\n    def prompts_dir(self) -&gt; Path:\n        \"\"\"Path to the prompts subdirectory.\"\"\"\n        return self.vault_path / self.prompts_dir_name\n\n    def validate_vault(self) -&gt; None:\n        \"\"\"Validate that the vault exists and is properly structured.\n\n        Raises:\n            VaultNotFoundError: If vault_path does not exist\n            VaultInvalidError: If vault exists but has no Prompts directory\n        \"\"\"\n        from promptkeep.exceptions import VaultNotFoundError, VaultInvalidError\n\n        if not self.vault_path.exists():\n            raise VaultNotFoundError([self.vault_path])\n        if not self.prompts_dir.exists() or not self.prompts_dir.is_dir():\n            raise VaultInvalidError(self.vault_path)\n</code></pre>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#protocolspy-service-interfaces","title":"<code>protocols.py</code> - Service Interfaces","text":"<p>Note: Uses <code>List</code> from typing for Python 3.8 compatibility (matching existing codebase style).</p> <pre><code>from pathlib import Path\nfrom typing import List, Optional, Protocol\n\nfrom promptkeep.models import Prompt\n\nclass ClipboardService(Protocol):\n    \"\"\"Interface for clipboard operations.\"\"\"\n    def copy(self, text: str) -&gt; None: ...\n\nclass EditorService(Protocol):\n    \"\"\"Interface for opening files in an editor.\n\n    Implementations should raise EditorNotFoundError if the editor binary\n    is not found, or EditorError for other failures.\n    \"\"\"\n    def open(self, file_path: Path) -&gt; bool: ...\n\nclass PromptSelector(Protocol):\n    \"\"\"Interface for interactive prompt selection.\n\n    Implementations should raise SelectorNotFoundError if the selection\n    tool (e.g., fzf) is not installed.\n    \"\"\"\n    def select(self, items: List[Path], prompt: str) -&gt; Optional[Path]: ...\n\nclass PromptRepositoryProtocol(Protocol):\n    \"\"\"Interface for prompt storage operations.\n\n    This is a read-heavy interface reflecting the primary use case of\n    browsing and selecting prompts. Write operations (save) are secondary.\n    Delete is intentionally omitted - users manage files directly.\n\n    Exceptions:\n        get_by_path may raise FileNotFoundError if the file doesn't exist,\n        or IOError/OSError for permission issues.\n    \"\"\"\n    def list_all(self) -&gt; List[Prompt]: ...\n    def filter_by_tags(self, tags: List[str]) -&gt; List[Prompt]: ...\n    def get_by_path(self, filepath: Path) -&gt; Prompt: ...\n    def get_file_paths(self, tags: Optional[List[str]] = None) -&gt; List[Path]: ...\n    def save(self, prompt: Prompt, filename: Optional[str] = None) -&gt; Path: ...\n    def exists_similar(self, title: str) -&gt; List[Path]: ...\n</code></pre>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#exceptionspy-exception-hierarchy","title":"<code>exceptions.py</code> - Exception Hierarchy","text":"<pre><code>from pathlib import Path\nfrom typing import List, Optional\n\nclass PromptKeepError(Exception):\n    \"\"\"Base exception for all PromptKeep errors.\"\"\"\n    pass\n\nclass VaultNotFoundError(PromptKeepError):\n    \"\"\"Raised when no vault directory can be found.\"\"\"\n    def __init__(self, searched_paths: Optional[List[Path]] = None):\n        self.searched_paths = searched_paths or []\n        super().__init__(\"No vault found\")\n\nclass VaultInvalidError(PromptKeepError):\n    \"\"\"Raised when vault exists but is malformed (missing Prompts dir).\"\"\"\n    def __init__(self, vault_path: Path):\n        self.vault_path = vault_path\n        super().__init__(f\"Invalid vault at {vault_path}: missing Prompts directory\")\n\nclass EditorError(PromptKeepError):\n    \"\"\"Raised when the editor fails to open or exits with error.\"\"\"\n    pass\n\nclass EditorNotFoundError(EditorError):\n    \"\"\"Raised when the configured editor is not found.\"\"\"\n    def __init__(self, editor: str):\n        self.editor = editor\n        super().__init__(f\"Editor not found: {editor}\")\n\nclass SelectorError(PromptKeepError):\n    \"\"\"Raised when the prompt selector fails.\"\"\"\n    pass\n\nclass SelectorNotFoundError(SelectorError):\n    \"\"\"Raised when fzf is not installed.\"\"\"\n    def __init__(self):\n        super().__init__(\"fzf not found. Please install fzf to use this feature.\")\n</code></pre>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#servicespy-concrete-implementations","title":"<code>services.py</code> - Concrete Implementations","text":"<pre><code>import shlex\nimport subprocess\nfrom pathlib import Path\nfrom typing import List, Optional\n\nimport pyperclip\n\nfrom promptkeep.exceptions import (\n    EditorError,\n    EditorNotFoundError,\n    SelectorNotFoundError,\n)\n\nclass SystemClipboard:\n    \"\"\"Clipboard implementation using pyperclip.\"\"\"\n\n    def copy(self, text: str) -&gt; None:\n        pyperclip.copy(text)\n\nclass SystemEditor:\n    \"\"\"Editor implementation using subprocess.\"\"\"\n\n    def __init__(self, editor_command: str):\n        self.editor_command = editor_command\n\n    def open(self, file_path: Path) -&gt; bool:\n        try:\n            editor_parts = shlex.split(self.editor_command)\n            editor_parts.append(str(file_path))\n            subprocess.run(editor_parts, check=True)\n            return True\n        except FileNotFoundError:\n            raise EditorNotFoundError(self.editor_command)\n        except subprocess.CalledProcessError as e:\n            raise EditorError(f\"Editor exited with code {e.returncode}\")\n        except ValueError as e:\n            raise EditorError(f\"Invalid editor command: {e}\")\n\nclass FzfPromptSelector:\n    \"\"\"Prompt selector using fzf.\n\n    The preview_script is passed directly to fzf's --preview flag.\n    It should contain {} as a placeholder where fzf will substitute\n    the currently highlighted filename (this is standard fzf behavior).\n    \"\"\"\n\n    def __init__(self, preview_script: str):\n        self.preview_script = preview_script\n\n    def select(self, items: List[Path], prompt: str) -&gt; Optional[Path]:\n        if not items:\n            return None\n\n        try:\n            result = subprocess.check_output(\n                [\"fzf\", \"--prompt\", prompt, \"--preview\", self.preview_script],\n                input=\"\\n\".join(str(f) for f in items).encode(),\n            )\n            return Path(result.decode().strip())\n        except subprocess.CalledProcessError:\n            # User cancelled selection (Ctrl+C or Esc)\n            return None\n        except FileNotFoundError:\n            raise SelectorNotFoundError()\n</code></pre>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#constantspy-updated","title":"<code>constants.py</code> - Updated","text":"<p>Add the FZF preview script here to avoid circular imports:</p> <pre><code>\"\"\"Constants used throughout PromptKeep.\"\"\"\n\n# ... existing constants ...\n\n# FZF preview script - extracts title, tags and content from prompt files\n# The {} placeholder is substituted by fzf with the currently highlighted file\nFZF_PREVIEW_SCRIPT = \"\"\"awk '\n    BEGIN { in_yaml=0; printed_header=0 }\n    /^---$/ { in_yaml = !in_yaml; next }\n    in_yaml {\n        if ($1 == \"title:\") title = substr($0, 8)\n        if ($1 == \"tags:\") { tags=1; next }\n        if (tags &amp;&amp; $1 == \"-\") tag_list = tag_list \", \" substr($0, 3)\n    }\n    !in_yaml &amp;&amp; !printed_header {\n        gsub(/\"/, \"\", title)\n        sub(/, /, \"\", tag_list)\n        print \"Title: \" title\n        if (tag_list) print \"Tags:  \" tag_list\n        print \"----------------------------------------\"\n        printed_header=1\n        next\n    }\n    !in_yaml { print }\n' {}'\"\"\"\n</code></pre>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#contextpy-application-context","title":"<code>context.py</code> - Application Context","text":"<pre><code>from dataclasses import dataclass\nfrom typing import Optional\n\nfrom rich.console import Console\n\nfrom promptkeep.config import Config\nfrom promptkeep.constants import FZF_PREVIEW_SCRIPT\nfrom promptkeep.protocols import (\n    ClipboardService,\n    EditorService,\n    PromptRepositoryProtocol,\n    PromptSelector,\n)\nfrom promptkeep.repository import PromptRepository\nfrom promptkeep.services import FzfPromptSelector, SystemClipboard, SystemEditor\n\n@dataclass\nclass AppContext:\n    \"\"\"Container for all application dependencies.\n\n    This is the composition root - the single place where concrete\n    implementations are wired together. Commands receive this context\n    and use the abstract interfaces, making them easy to test with mocks.\n    \"\"\"\n    config: Config\n    clipboard: ClipboardService\n    editor: EditorService\n    selector: PromptSelector\n    console: Console\n    repository: PromptRepositoryProtocol\n\n    @classmethod\n    def create_default(cls, config: Config) -&gt; \"AppContext\":\n        \"\"\"Create context with default production implementations.\"\"\"\n        return cls(\n            config=config,\n            clipboard=SystemClipboard(),\n            editor=SystemEditor(config.editor),\n            selector=FzfPromptSelector(FZF_PREVIEW_SCRIPT),\n            console=Console(),\n            repository=PromptRepository(config.vault_path),\n        )\n</code></pre>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#cli-integration-pattern","title":"CLI Integration Pattern","text":"<p>Typer callback to inject AppContext:</p> <pre><code># cli.py\n\nfrom typing import List, Optional\nimport typer\nfrom rich.console import Console\nfrom rich.panel import Panel\n\nfrom promptkeep.config import Config\nfrom promptkeep.context import AppContext\nfrom promptkeep.exceptions import (\n    VaultNotFoundError,\n    VaultInvalidError,\n    EditorError,\n    SelectorNotFoundError,\n)\nfrom promptkeep.utils import extract_prompt_content\n\napp = typer.Typer(help=\"PromptKeep - A CLI tool for managing AI prompts\")\nconsole = Console()  # Module-level console for error handling decorator\n\n@app.callback()\ndef main_callback(ctx: typer.Context) -&gt; None:\n    \"\"\"Initialize application context for all commands.\"\"\"\n    # Context is created lazily by commands that need it\n    # This callback just ensures ctx.obj exists\n    ctx.ensure_object(dict)\n\ndef get_context(vault_path: Optional[str] = None) -&gt; AppContext:\n    \"\"\"Get or create AppContext with optional vault override.\n\n    This helper is called by commands that need the full context.\n    Vault validation is the caller's responsibility via config.validate_vault().\n    \"\"\"\n    config = Config.from_environment(vault_override=vault_path)\n    return AppContext.create_default(config)\n\ndef handle_errors(func):\n    \"\"\"Decorator to convert PromptKeepError to user-friendly CLI output.\n\n    Uses the module-level console for error output.\n    \"\"\"\n    def wrapper(*args, **kwargs):\n        try:\n            return func(*args, **kwargs)\n        except VaultNotFoundError:\n            console.print(Panel.fit(\n                \"[red]Error: No vault found.[/]\\n\"\n                \"Use 'promptkeep init' or specify --vault.\",\n                title=\"Error\", border_style=\"red\",\n            ))\n            raise typer.Exit(1)\n        except VaultInvalidError as e:\n            console.print(Panel.fit(\n                f\"[red]Error: Invalid vault at {e.vault_path}[/]\",\n                title=\"Error\", border_style=\"red\",\n            ))\n            raise typer.Exit(1)\n        except EditorError as e:\n            console.print(Panel.fit(\n                f\"[red]Error: {e}[/]\",\n                title=\"Error\", border_style=\"red\",\n            ))\n            raise typer.Exit(1)\n        except SelectorNotFoundError:\n            console.print(Panel.fit(\n                \"[red]Error: fzf not found.[/]\\n\"\n                \"Please install fzf to use this command.\",\n                title=\"Error\", border_style=\"red\",\n            ))\n            raise typer.Exit(1)\n    return wrapper\n\n@app.command(\"pick\")\n@handle_errors\ndef pick_command(\n    vault_path: Optional[str] = typer.Option(None, \"--vault\", \"-v\"),\n    tags: Optional[List[str]] = typer.Option(None, \"--tag\", \"-t\"),\n) -&gt; None:\n    \"\"\"Select a prompt and copy to clipboard.\"\"\"\n    app_ctx = get_context(vault_path)\n    app_ctx.config.validate_vault()  # Raises VaultNotFoundError/VaultInvalidError if invalid\n\n    prompt_files = app_ctx.repository.get_file_paths(tags=list(tags) if tags else None)\n\n    if not prompt_files:\n        app_ctx.console.print(Panel.fit(\n            \"[yellow]No prompts found.[/]\",\n            title=\"Warning\", border_style=\"yellow\",\n        ))\n        raise typer.Exit(1)\n\n    selected = app_ctx.selector.select(prompt_files, \"Select a prompt: \")\n    if not selected:\n        raise typer.Exit(0)  # User cancelled\n\n    content = selected.read_text()\n    prompt_content = extract_prompt_content(content)\n    app_ctx.clipboard.copy(prompt_content)\n\n    app_ctx.console.print(Panel.fit(\n        \"Prompt copied to clipboard\",\n        title=\"Success\", border_style=\"green\",\n    ))\n</code></pre>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#modified-modules","title":"Modified Modules","text":""},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#clipy-changes","title":"<code>cli.py</code> Changes","text":"<ul> <li>Remove <code>_FZF_PREVIEW_SCRIPT</code> (moved to constants.py)</li> <li>Add <code>get_context()</code> helper and <code>handle_errors</code> decorator</li> <li>Refactor commands to use AppContext pattern shown above</li> <li>Replace inline error handling with exception catching via decorator</li> </ul>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#utilspy-changes","title":"<code>utils.py</code> Changes","text":"<ul> <li>Remove <code>open_editor()</code> - logic moved to <code>SystemEditor</code> service</li> <li>Remove <code>copy_to_clipboard()</code> - logic moved to <code>SystemClipboard</code> service</li> <li>Keep <code>sanitize_filename()</code>, <code>extract_prompt_content()</code> as pure utilities (still used)</li> <li>Remove <code>find_vault_path()</code>, <code>validate_vault_path()</code> - replaced by <code>Config.from_environment()</code> and <code>Config.validate_vault()</code>. No deprecation period needed since this is internal refactoring, not a public API change.</li> </ul>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#repositorypy-changes","title":"<code>repository.py</code> Changes","text":"<ul> <li>Ensure all methods have complete type hints using <code>List</code> from typing</li> <li>Verify compliance with <code>PromptRepositoryProtocol</code></li> <li>No structural changes needed</li> </ul>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#test-updates","title":"Test Updates","text":""},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#test_clipy-fixes","title":"<code>test_cli.py</code> Fixes","text":"<ul> <li>Remove unused <code>shutil</code> import</li> <li>Fix import sorting (stdlib, third-party, local)</li> </ul>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#new-test-files","title":"New Test Files","text":"<ul> <li><code>tests/test_config.py</code> - Configuration loading and validation tests</li> <li><code>tests/test_services.py</code> - Service implementation tests</li> <li><code>tests/test_context.py</code> - Context creation tests</li> <li><code>tests/test_exceptions.py</code> - Exception behavior tests</li> </ul>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#file-structure-after","title":"File Structure After","text":"<pre><code>src/promptkeep/\n\u251c\u2500\u2500 __init__.py\n\u251c\u2500\u2500 __main__.py\n\u251c\u2500\u2500 cli.py           # Commands using AppContext\n\u251c\u2500\u2500 config.py        # NEW: Configuration management\n\u251c\u2500\u2500 constants.py     # Updated with FZF_PREVIEW_SCRIPT\n\u251c\u2500\u2500 context.py       # NEW: AppContext wiring\n\u251c\u2500\u2500 exceptions.py    # NEW: Exception hierarchy\n\u251c\u2500\u2500 main.py\n\u251c\u2500\u2500 models.py\n\u251c\u2500\u2500 protocols.py     # NEW: Service interfaces\n\u251c\u2500\u2500 repository.py    # Minor type hint additions\n\u251c\u2500\u2500 services.py      # NEW: Concrete implementations\n\u2514\u2500\u2500 utils.py         # Slimmed down, pure utilities\n\ntests/\n\u251c\u2500\u2500 test_cli.py      # Fixed imports, updated for new patterns\n\u251c\u2500\u2500 test_config.py   # NEW\n\u251c\u2500\u2500 test_context.py  # NEW\n\u251c\u2500\u2500 test_exceptions.py # NEW\n\u251c\u2500\u2500 test_main.py\n\u251c\u2500\u2500 test_models.py\n\u251c\u2500\u2500 test_repository.py\n\u251c\u2500\u2500 test_services.py # NEW\n\u2514\u2500\u2500 test_utils.py\n</code></pre>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#what-this-demonstrates","title":"What This Demonstrates","text":"Pattern What It Shows Configuration dataclass Centralized config, immutability, clear precedence Protocol classes Structural typing, interface segregation Dependency injection Testability, loose coupling, SOLID principles Exception hierarchy Proper error handling, debuggability AppContext Composition root pattern, explicit dependencies Error handling decorator DRY principle, separation of concerns"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#implementation-order","title":"Implementation Order","text":"<ol> <li>Create <code>exceptions.py</code> (no dependencies)</li> <li>Create <code>protocols.py</code> (depends only on models)</li> <li>Update <code>constants.py</code> with FZF_PREVIEW_SCRIPT</li> <li>Create <code>config.py</code> (depends on constants, exceptions)</li> <li>Create <code>services.py</code> (depends on exceptions)</li> <li>Create <code>context.py</code> (depends on config, protocols, services, repository)</li> <li>Refactor <code>cli.py</code> to use context pattern</li> <li>Slim down <code>utils.py</code></li> <li>Fix test linting issues</li> <li>Add new test files</li> <li>Verify all tests pass with &gt;90% coverage</li> </ol>"},{"location":"superpowers/specs/2026-03-23-architecture-improvements-design/#success-criteria","title":"Success Criteria","text":"<ul> <li>All existing tests pass</li> <li>New tests for new modules</li> <li>Coverage remains above 90%</li> <li>Zero linting errors</li> <li>All type hints complete (mypy passes)</li> <li>README unchanged (no mention of refactoring motivation)</li> </ul>"}]}